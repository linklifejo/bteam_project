<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="wi">

<!-- 찜 정보등록 -->
<insert id='insert'>
insert into willgo( refid, wtype, member_id)
values (#{refid}, #{wtype}, #{member_id})
</insert>

<!-- 찜 정보 수정 -->
<update id='update'>
update willgo
set refid = #{refid}, wtype = #{wtype}, member_id = #{member_id}
where id = #{id}
</update>

<!-- 찜 정보 삭제 -->
<delete id='delete'>
delete from willgo where id = #{id}
</delete>

<!-- 찜 정보 조회 -->
<select id='info' resultType='willgo.WillgoVO'>
select * from willgo where id = #{id}
order by will_date
</select>


<!-- 찜 전체정보 조회 -->
<select id='list' resultType='willgo.WillgoVO'>
select * from willgo order by will_date
</select>
<!-- 찜 회원별 타입별 조회 -->
<select id='member_wtype_list' resultType='willgo.WillgoVO'>
select *
from willgo
where wype= #{wtype} and member_id= #{member_id}
 order by will_date
</select>


</mapper>  